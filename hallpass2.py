def main():

    # Add in variables needed for madlib

    madlib = """
    _""" + name + """_ is authorized to be
    at _""" + place + """_ instead of _""" + noun + """ class.
    """

    print(madlib)

main()